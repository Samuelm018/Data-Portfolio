services:
  - type: web
    name: portfolio-website
    env: node
    buildCommand: cd portfolio-website && npm install && npm run build
    startCommand: cd portfolio-website && npm start
    envVars:
      - key: NODE_VERSION
        value: 20
    autoDeploy: true
